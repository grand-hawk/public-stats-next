{
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24",
  "forwardPorts": [3000],
  "containerEnv": {
    "DATA_ENV": "production",
    "FORCE_CACHE": "true",
    "NEXT_PUBLIC_STACKBLITZ": "true",
    "NODE_ENV": "development"
  },
  "postCreateCommand": "sudo corepack enable && sudo corepack prepare pnpm@10 --activate && pnpm install --no-lockfile --ignore-scripts && pnpm generate && pnpm content:generate",
  "postStartCommand": "pnpm dev",
  "remoteUser": "node"
}
